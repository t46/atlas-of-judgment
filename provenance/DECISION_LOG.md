# 意思決定ログ（Decision Log）

出典は特記なき限り Codex セッション履歴 `/Users/s30825/.codex/sessions/2026/08/11/rollout-2026-08-11T18-17-08-019ff01c-83cb-7060-ac21-3658f8b4a748.jsonl`（以下「セッションJSONL」、約34MB・約2万行、成長中）。タイムスタンプはUTC（JSTは+9時間）。引用は要約・翻訳を含む場合があるため、一次証跡として再確認する際は `docs/provenance/SESSION_EVIDENCE_INDEX.md` の検索語で `rg` すること。ファイルサイズ・行数は 2026-08-18 時点のスナップショット（34,654,158バイト、約20,823行）。

## 2026-08-11: プロジェクト開始直後のスコープ決定

| 時刻(UTC) | 決定 | 根拠 |
|---|---|---|
| 09:35:12 | 情報源をOpenReviewに限定 | 「はい、まず公開範囲をOpenReviewに限定するのは賛成です。これなら出所と取得経路を統一できます。」 |
| 09:37:13 | ICLRを主コーパスに選定する根拠を確立 | 「最大の問題は、ICMLとNeurIPSでは不採択論文のレビューが基本的に公開されないことです…公開されているレビューは、採択論文に偏っています。」 |
| 09:38:17 | ICLRを主コーパスとする方針を確定 | 「はい、まずICLRを主コーパスにする方針で進めましょう。」 |
| 09:38:57 | 年範囲 2018–2026 を提案、2017年は除外 | 「2018以降は公式OpenReview venueとして確認しやすいので、まず2018〜2026を確実に押さえ、2017は別途調査するのがよさそうです。」 |
| 09:39:36 | 年範囲を確定 | 「了解です。ICLR Main Conference 2018–2026を対象に…」 |

**注記**: この年範囲の決定はセッション開始からわずか約4分後に行われている。`docs/iclr-2026-pilot.md` の「2026年全件のDirectを先に実行する」という記述は、この最初から固定された2018–2026という年範囲の中での、**分析手法ロールアウトの優先順序**についての決定であり、年範囲自体の後発的拡張ではない。

## 手法選択: Layered vs Direct

`docs/iclr-2026-pilot.md`（2026-08-11 22:05作成）: 同一モデルによるブラインド審判でLayeredが69–31でDirectに優勢だったにもかかわらず、コスト・来歴リスクの理由でDirectを先行採用。「Layeredは中間メモの誤りが後段に伝播しうる」という明示的な懸念が記載されている。この決定は**品質より低コスト・低リスクを優先した明示的なトレードオフ**であり、Directの成果物を読む際はLayeredより審査員評価が低かったことを踏まえる必要がある。

## 研究課題の転換: 「レビュー分析」から「reviewer logic」へ

`docs/evaluation-logic-discovery-pilot.md`（2026-08-16 07:15作成）が転換点。詳細は `docs/provenance/END_TO_END_PROCESS.md` §1 を参照。この転換以降、論文レベルの `paper_synthesis` からの抽出ではなく、査読者レベルの段階（`initial_blind`, `trajectory`）を抽出源として重視する方針に切り替わっている（`paper_synthesis` の取得率が71.2%に留まっていたことが直接の動機）。

## 2026-08-18: 24kリトライのマージと32kリトライの起動

（時刻はUTC、JSTは+9時間。以下はセッションJSONL終盤、約20,700行以降からの抜粋）

| 時刻(UTC) | 出来事 | 引用/根拠 |
|---|---|---|
| 04:03:33 | 24kリトライのマージ完了 | 「マージ完了。元データ、初回API使用量、リトライデータはすべて保持。対象: 51,813。厳密成功: 50,049 (96.60%)。警告付き含む完全JSON: 50,854 (98.15%)。完全JSONなし: 959 (切り詰め/パース失敗: 921, プロバイダエラー: 38)。Direct分析累計コスト: $73.439685。2026分析含む累計: 約$102.23…検証: source+retry DB整合性ともに`ok`、完全JSON出力50,854件すべて構文検証済み、検証JSON51,813件すべて構文検証済み、テスト68件全通過、残置一時ファイルなし…残る959件の追加課金は未実行。次は自然に921件を32k出力で再試行、38件のプロバイダエラーも回収する。追加コスト見積 ≈$3.5–4.5。」 |
| 04:10:33 | 32kリトライを既存データと隔離して開始する決定 | 「了解しました。残る959件を既存データと隔離した32kリトライとして開始し、並行してClaude Codeに引き継ぎ資料と『収集から処理まで』の完全監査可能なドキュメントを作らせます。」 |
| 04:11:39 | 選定ロジックの確定（二重課金回避） | 「残る959件は、24kリトライのうち完全な出力ファイルがまだ無いものだけを正確に選定できることを確認。警告はあるが既存JSONがある88件は二重課金回避のため除外。次は専用の32kディレクトリを作成し、上限を$6に固定して開始。」 |
| 04:12:14 | 起動直前の軽微な障害（コスト/データへの影響なし） | 「準備中、既存スクリプトの直接実行形式が`uv run`のモジュール検索パスと一致しないエラーが発生。これはAPI呼び出し前なのでコスト・データへの影響なし。モジュール実行形式に切り替えて継続。」 |
| 04:12:39 | 32kリトライ起動、資格情報方針の明言 | 「32kリトライを開始。959件中64件処理中、895件待機、コスト見積$3.81、上限$6。1Passwordは起動時に一度だけ読み込み、APIリクエストごとには読まない。」 |
| 04:13:58 | 本ドキュメント作成タスクの指示構造を確定 | 「Claude Code向けの作業指示を確定。単なる要約ではなく、2つの独立サブエージェント（引き継ぎ監査／来歴監査）が調査し、その後親Claudeが実ファイルと突き合わせて再検証しドキュメントを書く構成。session-reviewポリシーに従い、履歴は読み取り専用、成果物は`docs/handoff/`と`docs/provenance/`に限定。バックグラウンドで起動。」 |
| 04:14:30 | 個々のコンテンツモデレーション拒否は自動再試行しない方針の言明（run単位） | 「リトライ順調: 125件完了、64件処理中。5件はHTTP 400のコンテンツ検査エラーで出力長とは無関係なので、自動再試行はせず記録のみ。実コストは現在約$0.084。」 |
| 04:16:25 | マージスクリプト拡張の理由（ネストしたリトライ系譜対応） | 「マージ時の安全性も確認済み。この32kリトライは24kリトライの子であるため、既存のマージ機構ではそのまま元へマージできない。履歴を偽装したり24k結果を上書き扱いにしたりせず、リトライの親子関係を検証したうえで、32kの使用量・成功分を独立した試行として元に記録できるよう最小限拡張する。稼働中データには触れない。」 |
| 04:17:07 | マージ拡張のテスト修正（ロジックバグではない） | 「マージ拡張のテストで検出されたのは、macOSの一時ディレクトリのエイリアシング差異（`/var`と`/private/var`）のみ — ロジックバグではなく、テスト期待値の正規化漏れ。正規（解決済み）パスに揃えて再検証。」 |
| 04:17:51 | コンテンツモデレーション拒否をリトライループへ戻さない方針の確定、独立監査への言及 | 「32k側は276件完了。失敗28件のうち、19件はまだJSON切り詰め/不正、11件はコンテンツ検査、1件はエビデンス参照検証エラー（カウントタイミングによる重複の可能性）。これらは再試行ループへ戻さず、完了後に成功分だけ安全に統合します。」 |
| 04:31:44 | 32kリトライのマージ実施（`merged_at` タイムスタンプ、`merge-reviewer-logic-direct-qwen-retry-32k-v1.json` より直接確認、セッション発言の引用ではなくファイル内タイムスタンプ） | 32kリトライ完了（complete=812, failed=147, 実コスト$1.355473）後、`retry_ancestry` を32k→24k→full-v1のネスト系譜として検証したうえでマージを適用。適用前に `state-pre-retry-merge-20260818T043143Z.sqlite3` を自動バックアップ。適用後、`reviewer-logic-direct-qwen-full-v1` は `complete=50,861 (98.16%), failed=952` で確定。 |

## 2026-08-18 マージ後の確定結果（read-only検証、本ログ更新時点 2026-08-18T04:37:05Z）

`reviewer-logic-direct-qwen-full-v1` の状態カウント・マージレコード・バックアップファイルの存在を直接クエリ/読み込みで確認し、上記04:31:44の言明と完全に一致することを確認した:
- `state.sqlite3`: `complete=50861, failed=952`
- `retry_attempts`: `reviewer-logic-direct-qwen-retry-24k-v1=7270`, `reviewer-logic-direct-qwen-retry-32k-v1=959`
- `outputs/`: 51,674ファイル / 51,813対象（99.73%）
- 失敗952件の内訳（直接集計、確定値）: 出力JSONあり813件（無効なエビデンス参照709件、JSON Schema検証エラー65件、その他の厳密/ローカル検証エラー39件）、出力JSONなし139件（パース/切り詰め107件、プロバイダのコンテンツポリシー拒否32件）
- 単体テスト69件、全通過（`uv run python -m unittest discover -s tests`）
- プロセス・tmuxセッションともに残存なし（新規のQwenリトライ/課金プロセスは動いていない）

## 未解決・追跡すべき論点（本ログ時点）

- 残存952件（コンテンツモデレーション拒否32件を含む）を「Direct-Qwenトラックの最終カバレッジ（98.16%）」として正式に確定するかどうかは、セッション履歴上でも明示的な最終宣言としては確認できていない（UNVERIFIED）。次セッションでDirectorに確認することを推奨。
- 出力JSONあり813件（大半は"invalid evidence ref"709件）が `reprocess` サブコマンド（新規API課金なし）で救えるかどうかは、正規化/検証コードの改善が前提であり、本ログ時点では未着手・修復は保証されない（詳細は `docs/handoff/CLAUDE_CODE_HANDOFF.md` §5-b）。出力JSONなし139件（パース/切り詰め107件＋プロバイダのコンテンツポリシー拒否32件）は`reprocess`の対象外で、別の対処が必要（同§5-a）。
- DeepSeek用の1Password資格情報取得を自動化するzshラッパー（Qwen側の `run_qwen_direct_retry_32k_supervised.zsh` に相当するもの）が存在するかは今回の調査で確認できなかった（UNVERIFIED、`docs/handoff/CLAUDE_CODE_HANDOFF.md` §7参照）。
